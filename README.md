# PixelForge Nexus

This is a simple prototype for project management with role based access. It uses Flask with SQLite for storage and supports basic MFA using TOTP.

## Setup

```bash
pip install -r requirements.txt
python -m pixelforge.app
```

The application stores uploads in `pixelforge/uploads/`.
